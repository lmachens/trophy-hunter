import React from 'react';
import { GameChildProps } from '../../layouts/GameLayout';

const History = ({ onQueryChange }: GameChildProps) => {
  return <div>History</div>;
};

export default History;
