import { Level } from '../../../levels/types';
import { EpicIcon, EpicMarker } from '../../../levels/epic';

const epic: Level = {
  island: 'hub',
  name: 'epic',
  title: 'Epic',
  Icon: EpicIcon,
  Marker: EpicMarker,
  trophies: [
    /* */
  ]
};

export default epic;
