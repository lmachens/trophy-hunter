import { Level } from '../../../levels/types';
import { TeamplayIcon, TeamplayMarker } from '../../../levels/teamplay';

const teamplay: Level = {
  island: 'hub',
  name: 'teamplay',
  title: 'Teamplay',
  Icon: TeamplayIcon,
  Marker: TeamplayMarker,
  trophies: [
    /* */
  ]
};

export default teamplay;
