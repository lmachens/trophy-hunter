import React from 'react';
import { GameChildProps } from '../../layouts/GameLayout';

const Leaderboard = ({ onQueryChange }: GameChildProps) => {
  return <div>Leaderboard</div>;
};

export default Leaderboard;
